import 'package:flutter/material.dart';

class OrderNotificationPage extends StatelessWidget {
  const OrderNotificationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
